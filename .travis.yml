language: python
python:
  - "3.6"

before_install:
  - sudo apt-get update
  - sudo apt-get install -y libxml2-dev

install: 
  - pip install -r webapp/requirements.txt

script: 
  - webapp/tests/test.py
